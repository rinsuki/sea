extends layout

block content
    h1 Sea
    if session && session.user.inviteCode == null
        strong: a(href="/input_invite_code") 招待コードを入力するまでは、全ての機能が利用できません。ここをクリックすると招待コードの入力画面に推移します
    ul
        if session
            li: a(href="/applications") 公開されてるアプリ一覧
            li: a(href="/explore") Explore
                ul
                    li: a(href="/explore/ranking") ランキング
            li: a(href="/settings") 設定
            li: a(href="/logout") ログアウト
        else
            li: a(href="/login") ログイン
            li: a(href="/register") 登録
    
    if session
        table(width="95%", cellspacing=7, cellpadding=3, border=1, bgcolor="#CFC", align="center")
            tr: td: small: a(href="/posts/") 1: 雑談スレ★n (#{threadNumber})